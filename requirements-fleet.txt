# requirements-fleet.txt
# Pinned runtime dependencies for the SEISMOGRAPH first-party probe fleet.
# Update by running: pip install httpx cryptography && pip freeze | grep -E "^(httpx|cryptography)=="
# KNOWN-LIMIT-FLEET-002: pinned 2026-06-22
#
# DO NOT add dev/test dependencies here. This file is used by Dockerfile.fleet only.
httpx==0.28.1
cryptography==49.0.0
